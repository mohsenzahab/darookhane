import 'package:darookhane/app/core/values/screen_values.dart';
import 'package:flutter/material.dart';

final kDecorationForm =
    BoxDecoration(borderRadius: BorderRadius.circular(12), color: Colors.white);

final kBorderRadiusCircular = BorderRadius.circular(kBorderRadiusVal);
